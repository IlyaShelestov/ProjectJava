package Items.PCandLaptops.PC.HyperPC;

import Items.PCandLaptops.PC.PC;

public abstract class HyperPC extends PC {
    public HyperPC(String model, double price, String brand) {
        super(model, price, brand);
    }
}
